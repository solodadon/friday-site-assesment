// Ask for the user's name
let userName = prompt("What's your name?");

// Show an alert greeting the user
if (userName) {
    alert("Welcome, " + userName + "!");
} else {
    alert("Welcome, guest!");
}


